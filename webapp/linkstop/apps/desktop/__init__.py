
#from django.db.signals import pre_save
#
#def make_profile(model, instance, created):
#    if created:
#        profile = Profile.create(user=instance)
#
#
#pre_save.connect(make_profile, sender=User)
